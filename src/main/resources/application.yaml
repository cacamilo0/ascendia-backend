spring:
  application:
    name: ascendia

  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

server:
  port: ${PORT:8080}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}